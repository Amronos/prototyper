# Comms Bridge

## Local Development

Use the Nix shell so the expected tools are available:

```sh
nix develop
```

Install dependencies if needed:

```sh
bun install
```

Start the app:

```sh
bun run dev
```
